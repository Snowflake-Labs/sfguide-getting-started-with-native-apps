definition_version: 1
native_app:
  name: NATIVE_APP_QUICKSTART_PACKAGE
  source_stage: app_src.stage
  artifacts:
    - src: app/src/*
      dest: ./
  package:
    scripts:
      - app/src/scripts/setup-package-script.sql